# Recipe Card Photos

All 168 recipes have a card photo, extracted from the sheets in `recipe card photos/` and saved to `public/images/recipes/`.

(Sinangag was removed from the dataset as a duplicate of Garlic Fried Rice, so it never needed one.)
